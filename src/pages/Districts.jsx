import {useState} from 'react'; import {Search,ArrowRight} from 'lucide-react'; import {districts} from '../data/content'; import {SectionTitle} from '../components/Layout';
export default function Districts(){const [q,setQ]=useState('');const shown=districts.filter(x=>x.includes(q));return <main className="listing"><SectionTitle eyebrow="जिला निर्देशिका" title="बिहार के 38 जिले" desc="नाम से खोजें और हर जिले की विशिष्ट विरासत तक पहुँचें।"/><label className="searchbox"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="जिले का नाम खोजें…"/></label><div className="all-districts">{shown.map((d,i)=><a href={`/districts/${encodeURIComponent(d)}`} key={d}><span>{String(i+1).padStart(2,'0')}</span><b>{d}</b><small>जिला परिचय और दर्शनीय स्थल</small><ArrowRight/></a>)}</div></main>}
